#include "main.h"

/**
* _abs - compute the absolute value
*
* @n: int to computer as absoute
*
* Return: absolute value
*/

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}

}
